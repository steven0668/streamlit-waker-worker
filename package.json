{
	"name": "streamlit-waker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --test-scheduled",
		"start": "wrangler dev --test-scheduled"
	},
	"devDependencies": {
		"@cloudflare/puppeteer": "^1.0.4",
		"wrangler": "^4.46.0"
	}
}
