name: Streamlit App Wakeup

on:
  schedule:
    - cron: '0 0 * * *'  # UTC 00:00 → 北京 08:00
  workflow_dispatch:
    inputs:
      target_urls:
        description: 'Override URLs (comma-separated)'
        required: false

jobs:
  wakeup:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: |
          # 修复 Azure 镜像源问题 → 换成官方源
          sudo sed -i 's/azure.archive.ubuntu.com/archive.ubuntu.com/g' /etc/apt/sources.list
          sudo sed -i 's/security.ubuntu.com/archive.ubuntu.com/g' /etc/apt/sources.list

          sudo apt-get clean
          sudo apt-get update -qq

          # 安装 Chromium 依赖
          sudo apt-get install -y \
            ca-certificates fonts-liberation libasound2 libatk-bridge2.0-0 \
            libatk1.0-0 libc6 libcairo2 libcups2 libdbus-1-3 libexpat1 \
            libfontconfig1 libgbm1 libgcc1 libglib2.0-0 libgtk-3-0 libnspr4 \
            libnss3 libpango-1.0-0 libpangocairo-1.0-0 libstdc++6 libx11-6 \
            libx11-xcb1 libxcb1 libxcomposite1 libxcursor1 libxdamage1 \
            libxext6 libxfixes3 libxi6 libxrandr2 libxrender1 libxss1 \
            libxtst6 wget > /dev/null 2>&1

          # 安装 puppeteer（推荐用 package.json）
          if [ -f package.json ]; then
            npm ci
          else
            npm init -y
            npm install puppeteer@latest
          fi

      - name: Run wakeup script
        env:
          TARGET_URLS: ${{ inputs.target_urls || vars.TARGET_URLS || 'https://blank-app-0668-01.streamlit.app/' }}
          CLICK_DELAY: ${{ vars.CLICK_DELAY || '20000' }}
          HEADLESS: ${{ vars.HEADLESS || 'true' }}
        run: |
          echo "唤醒目标:"
          echo "$TARGET_URLS" | tr ',' '\n' | sed 's/^/  → /'
          echo "----------------------------------------"
          node runner-once.js

      - name: Failure Alert
        if: failure()
        run: |
          echo "唤醒失败！请检查上方日志。"
          exit 1
